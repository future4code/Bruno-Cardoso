//a.
//A entrada é um array de números, e as saídas são
//as médias que estão guardadas dentro da variável estatisticas

//b.

// type variaveisDaFuncao = {
//   numeros: number[];
//   soma: number;
//   estatisticas: Object;
// };

//C.

// type amostraDeIdades = {
//     numeros: number[],
//     obterEstatisticas: void
// }

// function obterEstatisticas(numeros: number[]) {
//   const numerosOrdenados = numeros.sort((a, b) => a - b);

//   let soma: number = 0;

//   for (let num of numeros) {
//     soma += num;
//   }

//   const estatisticas: Object = {
//     maior: numerosOrdenados[numeros.length - 1],
//     menor: numerosOrdenados[0],
//     media: soma / numeros.length,
//   };

//   return estatisticas;
// }
